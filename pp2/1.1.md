# Step 1.1: Backend Skeleton & Directory Architecture (Code Implementation)

## Overview
This step implements the FastAPI backend skeleton. We will create the actual directory structure, define the configuration, and write the `main.py` entry point. This ensures a clean, modular architecture matching the paper's requirement for a production-ready ML service.

## 1. Directory Setup (Run in Terminal)
```bash
cd c:\Users\Admin\Documents\DL_IN_PAT\localhost\backend
mkdir app
mkdir app\api app\api\routes app\core app\services app\ml app\ml\models app\ml\metrics app\ml\training app\ml\inference app\workers app\schemas app\utils
type nul > app\__init__.py
```

## 2. Requirements File (`backend/requirements.txt`)
Create this file to manage dependencies exactly as required by the paper and models.
```text
fastapi==0.103.1
uvicorn==0.23.2
pydantic==2.3.0
pydantic-settings==2.0.3
torch==2.0.1
torchvision==0.15.2
piq==0.8.0
celery==5.3.4
redis==5.0.0
opencv-python==4.8.0.76
scikit-image==0.21.0
pandas==2.1.0
python-multipart==0.0.6
```

## 3. Core Configuration (`app/core/config.py`)
This file handles environment variables and CORS.
```python
from pydantic_settings import BaseSettings

class Settings(BaseSettings):
    PROJECT_NAME: str = "PAT-IQA Web Platform"
    API_V1_STR: str = "/api/v1"
    
    # Origins for Next.js frontend
    BACKEND_CORS_ORIGINS: list[str] = ["http://localhost:3000", "http://127.0.0.1:3000"]
    
    # Path mappings
    MODEL_DIR: str = "../models"
    DATA_DIR: str = "../data"
    SAVED_MODELS_DIR: str = "../saved_models"

    class Config:
        case_sensitive = True

settings = Settings()
```

## 4. Main FastAPI Entry Point (`app/main.py`)
This ties the application together.
```python
from fastapi import FastAPI
from fastapi.middleware.cors import CORSMiddleware
from app.core.config import settings
# from app.api.routes import comparator, training, inference, datasets

app = FastAPI(
    title=settings.PROJECT_NAME,
    openapi_url=f"{settings.API_V1_STR}/openapi.json"
)

# Set all CORS enabled origins
if settings.BACKEND_CORS_ORIGINS:
    app.add_middleware(
        CORSMiddleware,
        allow_origins=[str(origin) for origin in settings.BACKEND_CORS_ORIGINS],
        allow_credentials=True,
        allow_methods=["*"],
        allow_headers=["*"],
    )

@app.get("/health", tags=["Health"])
def health_check():
    return {"status": "ok", "message": "PAT-IQA Backend is running."}

# We will include routers as we build them:
# app.include_router(comparator.router, prefix=settings.API_V1_STR, tags=["Comparator"])
```
